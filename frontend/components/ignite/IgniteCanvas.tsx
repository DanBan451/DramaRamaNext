/**
 * Ignite canvas — uses the shared Forge {@link ../canvas/Canvas} with Ignite
 * API wiring on the parent page. Re-export for a stable import path.
 */
export { default } from "../canvas/Canvas";
